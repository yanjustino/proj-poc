${schema_conventions}

Com base no conteúdo da wiki, dos requisitos e dos atributos de qualidade abaixo, produza os diagramas relevantes dentre: `c4-context`, `c4-container`, `c4-component`, `process-flow`, `data-flow`, `sequence` e `state`. Produza ao menos `c4-context`. Para os três tipos C4, use Mermaid `flowchart` e as convenções C4Model do catálogo: person #08427B, system #1168BD, external #999999, container/database #438DD5 e component #85BBF0. Use `Strategic` para contexto/contêiner, `Product` para componente/process/data e `Tactical` para sequência/estado. Não use as diretivas experimentais `C4Context`/`C4Container`.

## Requisitos já gerados

${requisitos_content}

## Atributos de qualidade já gerados

${atributos_content}

## Conteúdo da wiki

${wiki_content}

${feedback_content}
