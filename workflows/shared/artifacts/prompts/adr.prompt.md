${schema_conventions}

Com base nos requisitos e nos atributos de qualidade abaixo, identifique as decisões arquiteturais relevantes já tomadas ou implícitas neles — cada uma com contexto, a decisão em si, e suas consequências. Só inclua decisões que os requisitos e atributos realmente sustentem; não invente arquitetura que não foi discutida.

## Requisitos já gerados

${requisitos_content}

## Atributos de qualidade já gerados

${atributos_content}

${feedback_content}
