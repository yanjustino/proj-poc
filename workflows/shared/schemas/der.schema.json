{
  "type": "object",
  "additionalProperties": false,
  "required": ["descricao", "fontes", "diagrama_mermaid"],
  "properties": {
    "descricao": {
      "type": "string",
      "description": "Parágrafo(s) descrevendo as entidades principais e como se relacionam."
    },
    "fontes": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "Referências que sustentam este modelo de dados."
    },
    "diagrama_mermaid": {
      "type": "string",
      "description": "Diagrama entidade-relacionamento em sintaxe Mermaid (erDiagram), com as entidades e relacionamentos principais."
    }
  }
}
