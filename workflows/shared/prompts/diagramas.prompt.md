${schema_conventions}

Com base no conteúdo da wiki, dos requisitos e dos atributos de qualidade abaixo, produza os diagramas C4 model relevantes para esta oportunidade (ao menos um diagrama de Contexto) em sintaxe Mermaid.

## Requisitos já gerados

${requisitos_content}

## Atributos de qualidade já gerados

${atributos_content}

## Conteúdo da wiki

${wiki_content}
